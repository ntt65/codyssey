Summary
최종평가 결과: 89%(16/18)

학습자는 터미널 명령어, Docker 설치 및 활용, Git 설정 등 기능 동작 검증 항목을 훌륭하게 수행하였습니다. 포트 매핑 및 볼륨 마운트의 개념을 실제 실습과 함께 명확하게 설명하였으나, 이미지와 컨테이너의 차이에 대한 개념적 설명 및 미션 중 어려웠던 점에 대한 심층적인 회고는 일부 누락되었습니다.

평가기준 1 : 터미널에서 기본 명령어로 폴더/파일 생성·이동·삭제를 수행한 흔적이 있는가?
결과: Pass

사유:

터미널에서 mkdir, touch, cp, mv, rm 명령어를 사용하여 디렉토리 및 파일을 조작한 내역이 명확히 확인됩니다.
평가기준 2 : 파일 권한 변경 결과가 확인되는가?
결과: Pass

사유:

chmod 744 sample.txt 명령어를 통해 파일 권한을 변경하고, ls -l로 변경 전후 결과를 비교한 내역이 확인됩니다.
평가기준 3 : docker --version이 출력되고, Docker가 동작 가능한 상태인가?
결과: Pass

사유:

docker --version 및 docker info 명령어를 실행하여 정상적으로 버전 및 상태가 출력된 점이 확인됩니다.
평가기준 4 : docker run hello-world가 정상 실행되는가?
결과: Pass

사유:

hello-world 컨테이너 실행 로그와 정상 동작 메시지가 기록되어 있습니다.
평가기준 5 : 이미지/컨테이너 목록 확인 및 정리 흔적이 있는가?
결과: Pass

사유:

docker images, docker ps -a, docker rm -f 명령어를 활용하여 목록 조회 및 컨테이너 삭제 작업을 수행한 내역이 존재합니다.
평가기준 6 : Dockerfile로 이미지 빌드가 가능한가?
결과: Pass

사유:

docker build -t my-web-img . 명령어를 사용하여 성공적으로 커스텀 이미지를 빌드한 내역이 확인됩니다.
평가기준 7 : 매핑된 포트로 접속이 가능한가?
결과: Pass

사유:

curl -s http://localhost:8080 명령어를 통해 포트 매핑 후 정상적으로 웹 페이지가 응답하는 것을 검증하였습니다.
평가기준 8 : Docker 볼륨 데이터가 컨테이너 삭제 후에도 유지되는가?
결과: Pass

사유:

docker volume create 후 컨테이너를 삭제하고 다시 실행했을 때 데이터가 유지됨을 직접 실습하여 증명하였습니다.
평가기준 9 : Git 설정 및 GitHub 연동이 확인되는가?
결과: Pass

사유:

git config, git init, add, commit, push 명령어를 활용하여 저장소 설정 및 연동 과정을 명시하였습니다.
평가기준 10 : 프로젝트 디렉토리 구조를 어떤 기준으로 구성했는지 설명할 수 있는가?
결과: Pass

사유:

프로젝트를 소스 코드(app/), 설정 파일(docker1/), 문서(docs/)로 구분하여 관리 효율성을 높였다는 기준을 제시하였습니다.
평가기준 11 : 포트/볼륨 설정을 어떤 방식으로 재현 가능하게 정리했는지 설명할 수 있는가?
결과: Pass

사유:

포트 포워딩(-p)과 바인드 마운트(-v)의 동작 원리와 명령어 구성 방식을 상세히 주석으로 설명하여 재현 가능성을 높였습니다.
평가기준 12 : ç
결과: Fail

사유:

베이스 이미지 간의 차이점은 설명하였으나, 빌드/실행/변경 관점에서 이미지와 컨테이너의 차이점에 대한 명확한 개념 설명이 부족합니다.
평가기준 13 : 컨테이너 내부 포트로 직접 접속할 수 없는 이유와 필요한 이유를 설명할 수 있는가?
결과: Pass

사유:

-p 옵션 설명에서 컨테이너 외부에서 내부로 접속할 통로를 뚫어주는 역할을 한다는 점을 통해 포트 매핑의 필요성을 잘 설명하였습니다.
평가기준 14 : 절대 경로/상대 경로를 어떤 상황에서 선택하는지 설명할 수 있는가?
결과: Pass

사유:

바인드 마운트 설명 시 $(pwd)가 현재 위치 전체 주소를 의미한다는 점을 명시하여 절대 경로의 활용 이유를 설명하였습니다.
평가기준 15 : 파일 권한 숫자 표기(예: 755, 644)가 어떤 규칙으로 결정되는지 설명할 수 있는가?
결과: Pass

사유:

rwx의 의미를 표로 정리하고, chmod 744 적용 결과를 통해 권한 표기 규칙을 이해하고 있음을 보여주었습니다.
평가기준 16 : “호스트 포트가 이미 사용 중”이라 포트 매핑이 실패한다면, 어떤 순서로 원인을 진단할지 설명할 수 있는가?
결과: Pass

사유:

포트 충돌 에러(port is already allocated) 발생 상황을 보여주고, docker rm -f로 기존 컨테이너를 삭제 후 재실행하는 조치 과정을 기록하였습니다.
평가기준 17 : 컨테이너 삭제 후 데이터가 사라진 경험이 있다면, 이를 방지하기 위한 대안을 설명할 수 있는가?
결과: Pass

사유:

컨테이너 삭제 시 일반 파일은 사라지지만, 마운트된 데이터는 유지된다는 점을 실습을 통해 비교 설명하였습니다.
평가기준 18 : 이 미션에서 가장 어려웠던 지점과, 해결 과정(가설 → 확인 → 조치)을 근거와 함께 설명할 수 있는가?
결과: Fail

사유:

미션 수행 중 발생한 가장 어려웠던 지점과 그 해결 과정에 대한 명시적인 회고 내용이 누락되어 있습니다.